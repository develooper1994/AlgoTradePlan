# Phase 02 — Core Contracts and Runtime Scaffolding

## Goal
Stabilize ids, types, contracts, runtime config, and event foundations.

## Requirements
- keep core contracts small and deterministic
- ensure config and event-store scaffolds support later plugins

## Validation Checklist
- [ ] core modules remain isolated from delivery-specific logic
- [ ] ids and config loaders have deterministic tests

## Tests
- `tests/unit/test_core_ids.py`
- `tests/unit/test_foundation_modules.py`
