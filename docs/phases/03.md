# Phase 03 — Test Harness and Hello-World Pipeline

## Goal
Maintain a fast validation path proving the repository can bootstrap and execute a trivial end-to-end flow.

## Requirements
- keep lint, test, and smoke commands stable
- preserve terminal and notebook smoke coverage

## Validation Checklist
- [ ] `make lint` passes
- [ ] `make test` passes
- [ ] `make smoke` passes

## Tests
- `tests/smoke/test_hello_world_pipeline.py`
