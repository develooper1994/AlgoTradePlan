# Onboarding in 10 Minutes

1. `make bootstrap`
2. `python scripts/run_tests.py`
3. `make smoke`
4. Open `notebooks/hello_world.ipynb`
5. Run `python scripts/hello_world_e2e.py --mode terminal --dry-run`

If failures occur, run with `--debug` and check redacted logs under `artifacts/`.
